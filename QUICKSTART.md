# 🚀 Minehunt AI - Quick Start Guide

## تشغيل البوت بخطوة واحدة

### الطريقة الأسهل (ملف واحد)

بعد حصولك على سيرفر ماينكرافت 1.20.1، اتبع هذه الخطوات:

```bash
# 1. اذهب إلى مجلد المشروع
cd /workspace

# 2. شغّل ملف البدء السريع
node quick-start.js
```

أو إذا كنت على Windows ويمكنك تشغيل ملفات `.js` بالنقر المزدوج:
- انقر نقراً مزدوجاً على `quick-start.js`

---

## ⚠️ المتطلبات الأساسية

قبل التشغيل، تأكد من:

### 1. تثبيت Node.js
```bash
node --version
# يجب أن يكون v20 أو أعلى
```

إذا لم يكن مثبتاً:
- حمّله من: https://nodejs.org/

### 2. سيرفر ماينكرافت 1.20.1

يجب أن يكون لديك سيرفر ماينكرافت يعمل:

```bash
# حمّل سيرفر 1.20.1 من:
https://www.minecraft.net/en-us/download/server/1.20.1

# استخرج الملف في مجلد منفصل
# شغّل السيرفر:
java -Xmx2G -Xms1G -jar server.jar nogui
```

### 3. الاتصال بالسيرفر

تأكد من أن السيرفر يعمل على:
- **العنوان:** `localhost`
- **المنفذ:** `25565`

إذا كان على جهاز آخر، عدّل `.env`:
```env
MC_HOST=192.168.1.100
MC_PORT=25565
```

---

## 📝 خطوات التشغيل الكاملة

### الخطوة 1: ثبّت الاعتمادات
```bash
cd /workspace
npm install
```

### الخطوة 2: عدّل الإعدادات (اختياري)
```bash
cp .env.example .env
nano .env
```

غيّر الهدف إذا أردت:
```env
TARGET_PLAYER=mr_xyous
```

### الخطوة 3: شغّل السيرفر
في نافذة طرفية منفصلة:
```bash
cd /path/to/minecraft-server
java -Xmx2G -Xms1G -jar server.jar nogui
```

انتظر حتى يظهر:
```
[Server thread/INFO]: Done!
```

### الخطوة 4: شغّل البوت
```bash
cd /workspace
node quick-start.js
```

### الخطوة 5: اتصل بالسيرفر
افتح ماينكرافت 1.20.1 واتصل بـ `localhost:25565` بحساب `mr_xyous`.

### الخطوة 6: راقب البوت
افتح المتصفح على:
```
http://localhost:50000
```

سترى لوحة التحكم تعرض:
- حالة البوت
- الصحة والطعام
- موقع الهدف وبُعده
- الحالة الحالية (PREPARATION, CHASE, COMBAT...)

---

## 🎯 ماذا يحدث بعد التشغيل؟

### الدقيقة الأولى (0-60 ثانية)
- البوت يجمع الخشب والحجر
- يصنع أدوات أساسية
- يحصل على طعام وسلاح
- **لا يهاجم الهدف** (قاعدة التحضير)
- يتتبع موقعك

### بعد الدقيقة الأولى
- إذا كنت قريباً: يبدأ المطاردة والقتال
- إذا كنت بعيداً: يستمر في التطوير ويخطط للاعتراض

### عند موتك
- يبدأ نافذة تطوير لمدة 5 دقائق
- يجمع ماس وحديد وموارد نادرة
- يصنع دروعاً وأسحلة أفضل
- ثم يعود للصيد

---

## 🔧 حل المشاكل

### "Connection timeout"
```
Error: Connection timeout - is the Minecraft server running?
```
**الحل:** تأكد أن سيرفر الماينكرافت يعمل قبل تشغيل البوت.

### "Port already in use"
```
Error: listen EADDRINUSE: address already in use :::50000
```
**الحل:** غيّر المنفذ في `.env`:
```env
DASHBOARD_PORT=50001
```

### "No module found"
```
Error: Cannot find module 'mineflayer'
```
**الحل:** شغّل:
```bash
npm install
```

---

## 📊 الأوامر السريعة

```bash
# تشغيل عادي
npm start

# وضع التطوير (مزيد من السجلات)
npm run dev

# تشغيل الاختبارات
npm test

# البدء السريع (موصى به)
node quick-start.js
```

---

## 🎮 نصائح للاستخدام

1. **ابدأ بسيرفر محلي** للتجربة قبل الاستخدام على سيرفر عام

2. **غيّر الهدف** في `.env` إذا أردت اختبار سلوك مختلف

3. **راقب لوحة التحكم** لفهم قرارات البوت

4. **جرّب وضعيات مختلفة:**
   - اختبئ في كهف
   - ابنِ منزلاً
   - حاول الذهاب إلى Nether
   - شاهد كيف يتصرف البوت

5. **تحقق من السجلات** في `logs/` لفهم السلوك

---

## 📞 الدعم

إذا واجهت مشكلة:
1. تحقق من السجلات
2. تأكد من إصدار ماينكرافت (1.20.1)
3. تأكد من تشغيل السيرفر
4. افتح issue على GitHub

---

**جاهز للصيد! 🎯⛏️**
